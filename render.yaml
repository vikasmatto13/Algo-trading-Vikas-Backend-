services:
  - type: web
    name: algoedge-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: UPSTOX_TOKEN
        value: PASTE_YOUR_DAILY_TOKEN_HERE
      - key: PAPER_TRADE
        value: "true"
